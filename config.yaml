model:
  embedding: "BAAI/bge-small-en-v1.5"
  image_llm: "minicpm-v" #image_desc.py

llm: #image_desc.py
  temperature: 0
  top_p: 0.3

chunking:
  chunk_size: 400
  overlap: 100

batch:
  image_processing_batch_size: 10 #for image processor.py
  embedding_batch_size: 32 #used in stage_2.py

device:
  type: "cuda"

paths:
  data: "data"
  embeddings: "embeddings.npy"
  chunks: "chunks.json"
  watcher: "E:\\Coding\\search_sample\\data" #watcher.py

processing:
  max_retries: 3 #db_connection

embedding:
  show_progress: True

search:
  retrieve_queries : 5
